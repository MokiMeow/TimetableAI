// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove or update any experimental options that are not valid
};

module.exports = nextConfig;